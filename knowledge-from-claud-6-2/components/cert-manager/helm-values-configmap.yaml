apiVersion: v1
kind: ConfigMap
metadata:
  name: cert-manager-values
  namespace: cert-manager
data:
  values.yaml: |
    installCRDs: true
    replicaCount: 1
    global:
      rbac:
        create: true
      logLevel: 2
      leaderElection:
        namespace: cert-manager